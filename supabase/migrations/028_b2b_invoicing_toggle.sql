-- ══════════════════════════════════════════════════════════════════
-- 028 — مفتاح تشغيل/إيقاف فواتير B2B من الواجهة
-- ────────────────────────────────────────────────────────────────
-- يضيف عمود على app_settings (نفس الجدول المقروء أصلاً من صفحة
-- إعدادات المتجر ومن NewOrder.jsx لكلا التبويبين). القيمة true
-- افتراضياً حتى لا يتغيّر أي سلوك حالي فور تطبيق الميجريشن؛ التبديل
-- الفعلي يتم من صفحة "الإعدادات" بالواجهة، وينعكس فوراً (RealtimeSync
-- يراقب app_settings أصلاً).
-- ══════════════════════════════════════════════════════════════════

alter table app_settings
  add column if not exists b2b_invoicing_enabled boolean not null default true;
